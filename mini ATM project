#include<stdio.h>
#include <string.h>
#include<stdlib.h>
int i,j,amount,deamount,choice,withamount,oldpin=7321,pin,newpin,DOB1,DOB=21102007;
char name,Name[100]="MP JITENDRA GOUD";
void Deposit()
{
  printf("----Welcome to our bank?----\n");
  printf("Enter your pin::");
  scanf("%d",&pin);
  if(oldpin==pin)
  {
    printf("\nHow much amount you want to deposit:");
    printf("\nEnter amount::");
    scanf("%d",&deamount);
    amount+=deamount;
  }
   else
  {
    printf("You entered wrong pin");
    printf("Please try again?");
  }
  printf("You have deposited your amount\n\tTHANK YOU AND VISIT AGAIN\n");
}
void Withdraw()
{
  printf("----Welcome to our bank-----\n");
  
  printf("Enter your pin::");
  scanf("%d",&pin);
  if(oldpin==pin)
  {
    printf("How much amount you want to withdraw:");
    printf("\nEnter amount::");
    scanf("%d",&withamount);
    if(withamount<amount)
    {
        amount=amount-withamount;
        printf("\nCollect your money");
    }
    else
    {
        printf("You do not have sufficient amount");   
    }
    
  }
  else
  {
    printf("You entered wrong pin");
    printf("Please try again?");
  }

}
void Balance_Enquiry()
{
  printf("----Welcome to our bank-----\n");
  
  printf("Enter your pin::");
  scanf("%d",&pin);
  if(oldpin==pin)
  {
      printf("Your account contains :  $%d ",amount);
  }
   else
  {
    printf("You entered wrong pin");
    printf("Please try again?");
  }
  printf("\n\tTHANK YOU AND VISIT AGAIN\t");
}
void Change_pin()
{
  char name[50];

  printf("----Welcome to our bank-----\n");
  printf("Enter your pin: ");
  scanf("%d",&pin);

  if(oldpin == pin) 
  {
      printf("Enter your new PIN: ");
      scanf("%d",&newpin);
      oldpin = newpin;
  }
  else
  {
      printf("Wrong PIN!\n");
      printf("If you forgot PIN:\n");

      printf("Enter your name (Aadhar): ");
      scanf("%s", name);

      printf("Enter your DOB (DDMMYYYY): ");
      scanf("%d",&DOB1);

      if(strcmp(Name, name) == 0 && DOB1 == DOB)
      {
          printf("Enter new PIN: ");
          scanf("%d",&newpin);
          oldpin = newpin;
          printf("PIN changed successfully!\n");
      }
      else 
      {
          printf("Verification failed. Visit bank.\n");
          exit(0);
      }
  }
}
void main()
{
  int ch;
  while(1)
  {
    printf("\n----WELCOME TO KARNATAKA BANK----");
    printf("\n1.Deposit\n2.Withdraw\n3.Balance Enquriy\n4.Change pin\n5.Exit\n");
    printf("Enter your choice::");
    scanf("%d",&ch);
    switch(ch)
    {
      case 1: Deposit();
              break;
      case 2: Withdraw();
              break;
      case 3: Balance_Enquiry();
              break;
      case 4: Change_pin();
              break;
      case 5: exit(0);
      default: printf("?Invalid choice?");
      
    }
  }
}
